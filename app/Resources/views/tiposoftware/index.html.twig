    {% extends ':template:template.html.twig' %}


    {% block contentheader %}


    <section class="content-header">
        <h1>
            Tiposoftware
            <small>Listar</small>
        </h1>
    </section>


    {% endblock %}


    {% block panelheading %}
    <a href="{{ path('cadastro_tiposoftware_new') }}" class="btn btn-primary btn-sm">
            <span class="glyphicon glyphicon-plus"></span>
            Novo
        </a>    {% endblock %}

    {% block panelbody %}
    <div class="table-responsive">
        <table class="table table-bordered table-condensed table-hover table-striped">
            <thead>
            <tr>
                <th>Ações</th>
                    <th>Descricao</th>
                    <th>Id</th>
            </tr>
            </thead>
            <tbody>
            {% for tiposoftware in tiposoftwares %}
            <tr>
                        <td><a href="{{ path('cadastro_tiposoftware_show', { 'id': tiposoftware.id }) }}">{{ tiposoftware.descricao }}</a></td>
                        <td>{{ tiposoftware.id }}</td>
                        <td>
                                <a href="{{ path('cadastro_tiposoftware_show', { 'id': tiposoftware.id }) }}" class="btn btn-primary btn-sm">
                                    <span class="glyphicon glyphicon-show"></span>
                                    show
                                </a>
                                <a href="{{ path('cadastro_tiposoftware_edit', { 'id': tiposoftware.id }) }}" class="btn btn-primary btn-sm">
                                    <span class="glyphicon glyphicon-edit"></span>
                                    edit
                                </a>
                        </td>
            </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>


    {% endblock %}
