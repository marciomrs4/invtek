    {% extends ':template:template.html.twig' %}


    {% block contentheader %}


    <section class="content-header">
        <h1>
            Tipomovimentacao
            <small>Listar</small>
        </h1>
    </section>


    {% endblock %}


    {% block panelheading %}
    <a href="{{ path('cadastro_tipomovimentacao_new') }}" class="btn btn-primary btn-sm">
            <span class="glyphicon glyphicon-plus"></span>
            Novo
        </a>    {% endblock %}

    {% block panelbody %}
    <div class="table-responsive">
        <table class="table table-bordered table-condensed table-hover table-striped">
            <thead>
            <tr>
                <th>Ações</th>
                    <th>Nome</th>
                    <th>Id</th>
            </tr>
            </thead>
            <tbody>
            {% for tipomovimentacao in tipomovimentacaos %}
            <tr>
                        <td><a href="{{ path('cadastro_tipomovimentacao_show', { 'id': tipomovimentacao.id }) }}">{{ tipomovimentacao.nome }}</a></td>
                        <td>{{ tipomovimentacao.id }}</td>
                        <td>
                                <a href="{{ path('cadastro_tipomovimentacao_show', { 'id': tipomovimentacao.id }) }}" class="btn btn-primary btn-sm">
                                    <span class="glyphicon glyphicon-show"></span>
                                    show
                                </a>
                                <a href="{{ path('cadastro_tipomovimentacao_edit', { 'id': tipomovimentacao.id }) }}" class="btn btn-primary btn-sm">
                                    <span class="glyphicon glyphicon-edit"></span>
                                    edit
                                </a>
                        </td>
            </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>


    {% endblock %}
