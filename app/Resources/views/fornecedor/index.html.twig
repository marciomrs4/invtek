    {% extends ':template:template.html.twig' %}


    {% block contentheader %}


    <section class="content-header">
        <h1>
            Fornecedor
            <small>Listar</small>
        </h1>
    </section>


    {% endblock %}


    {% block panelheading %}
    <a href="{{ path('cadastro_fornecedor_new') }}" class="btn btn-primary btn-sm">
            <span class="glyphicon glyphicon-plus"></span>
            Novo
        </a>    {% endblock %}

    {% block panelbody %}
    <div class="table-responsive">
        <table class="table table-bordered table-condensed table-hover table-striped">
            <thead>
            <tr>
                <th>Ações</th>
                    <th>Nome</th>
                    <th>Id</th>
            </tr>
            </thead>
            <tbody>
            {% for fornecedor in fornecedors %}
            <tr>
                        <td><a href="{{ path('cadastro_fornecedor_show', { 'id': fornecedor.id }) }}">{{ fornecedor.nome }}</a></td>
                        <td>{{ fornecedor.id }}</td>
                        <td>
                                <a href="{{ path('cadastro_fornecedor_show', { 'id': fornecedor.id }) }}" class="btn btn-primary btn-sm">
                                    <span class="glyphicon glyphicon-show"></span>
                                    show
                                </a>
                                <a href="{{ path('cadastro_fornecedor_edit', { 'id': fornecedor.id }) }}" class="btn btn-primary btn-sm">
                                    <span class="glyphicon glyphicon-edit"></span>
                                    edit
                                </a>
                        </td>
            </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>


    {% endblock %}
