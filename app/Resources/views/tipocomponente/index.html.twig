    {% extends ':template:template.html.twig' %}


    {% block contentheader %}


    <section class="content-header">
        <h1>
            Tipocomponente
            <small>Listar</small>
        </h1>
    </section>


    {% endblock %}


    {% block panelheading %}
    <a href="{{ path('cadastro_tipocomponente_new') }}" class="btn btn-primary btn-sm">
            <span class="glyphicon glyphicon-plus"></span>
            Novo
        </a>    {% endblock %}

    {% block panelbody %}
    <div class="table-responsive">
        <table class="table table-bordered table-condensed table-hover table-striped">
            <thead>
            <tr>
                <th>Ações</th>
                    <th>Descricao</th>
                    <th>Id</th>
            </tr>
            </thead>
            <tbody>
            {% for tipocomponente in tipocomponentes %}
            <tr>
                        <td><a href="{{ path('cadastro_tipocomponente_show', { 'id': tipocomponente.id }) }}">{{ tipocomponente.descricao }}</a></td>
                        <td>{{ tipocomponente.id }}</td>
                        <td>
                                <a href="{{ path('cadastro_tipocomponente_show', { 'id': tipocomponente.id }) }}" class="btn btn-primary btn-sm">
                                    <span class="glyphicon glyphicon-show"></span>
                                    show
                                </a>
                                <a href="{{ path('cadastro_tipocomponente_edit', { 'id': tipocomponente.id }) }}" class="btn btn-primary btn-sm">
                                    <span class="glyphicon glyphicon-edit"></span>
                                    edit
                                </a>
                        </td>
            </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>


    {% endblock %}
